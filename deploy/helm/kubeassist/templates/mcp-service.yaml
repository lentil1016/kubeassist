apiVersion: v1
kind: Service
metadata:
  name: kubeassist-mcp
  namespace: {{ .Release.Namespace }}
  labels:
    app: kubeassist-mcp
    {{- include "kubeassist.labels" . | nindent 4 }}
spec:
  selector:
    app: kubeassist-mcp
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
