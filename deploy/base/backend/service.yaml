apiVersion: v1
kind: Service
metadata:
  name: kubeassist-backend
  namespace: kubeassist
spec:
  selector:
    app: kubeassist-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
